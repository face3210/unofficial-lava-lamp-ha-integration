# https://www.home-assistant.io/integrations/homeassistant/
homeassistant:
  debug: true

# Keep local development focused on this integration. `default_config` starts a
# large set of unrelated integrations that pull optional runtime dependencies.
frontend:
config:
history:
logbook:
automation: !include automations.yaml
script: !include scripts.yaml
scene: !include scenes.yaml

# https://www.home-assistant.io/integrations/logger/
logger:
  default: info
  logs:
    custom_components.lava_lamp: debug

demo: